# CIRISBilling Monthly Archive
# Runs on the 2nd of each month at 3 AM UTC
# Archives the previous month's data to S3 Glacier

# Install this file:
#   sudo cp billing-archive.cron /etc/cron.d/billing-archive
#   sudo chmod 644 /etc/cron.d/billing-archive

SHELL=/bin/bash
PATH=/usr/local/bin:/usr/bin:/bin

# Archive previous month on the 2nd at 3 AM UTC
0 3 2 * * root /usr/bin/python3 /root/CIRISBilling/scripts/archive-billing.py >> /var/log/billing-archive.log 2>&1
